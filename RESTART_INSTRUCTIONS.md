# 🔄 تعليمات إعادة تشغيل التطبيق

## ✅ تم تحديث قاعدة البيانات والملفات الثابتة بنجاح!

---

## 📋 ما تم إنجازه:

✅ تحديث سجل CompanyInfo في قاعدة البيانات
- الاسم القديم: Elkhawaga
- الاسم الجديد: **ORIGIN.APP**

✅ تحديث سجل ContactFormSettings في قاعدة البيانات
- البريد القديم: info@elkhawaga.com
- البريد الجديد: **info@origin.app**

✅ مسح الـ Cache بنجاح
✅ جمع الملفات الثابتة (27 ملف تم نسخه، 287 غير متغير)

---

## 🚀 الخطوة الأخيرة: إعادة تشغيل الخادم

يعمل التطبيق حالياً باستخدام `python manage.py runserver`

### الطريقة 1: إعادة تشغيل بسيطة
في Terminal الذي يعمل فيه الخادم:
```bash
# اضغط Ctrl+C لإيقاف الخادم
# ثم أعد تشغيله:
python manage.py runserver
```

### الطريقة 2: إعادة تشغيل في الخلفية
```bash
# إيقاف الخادم الحالي
pkill -f "python manage.py runserver"

# انتظر ثانية
sleep 2

# إعادة التشغيل
cd /home/zakee/homeupdate
source venv/bin/activate
python manage.py runserver
```

### الطريقة 3: استخدام نص الإنتاج (إذا كنت في بيئة الإنتاج)
```bash
cd /home/zakee/homeupdate/لينكس
./run-production.sh
```

---

## 🔍 التحقق من التغييرات

بعد إعادة التشغيل، تحقق من:

1. **لوحة التحكم (Admin Panel)**:
   - افتح: http://localhost:8000/admin/
   - يجب أن ترى: "نظام إدارة ORIGIN.APP"

2. **صفحة معلومات الشركة**:
   - افتح: http://localhost:8000/accounts/company_info/
   - يجب أن ترى: "ORIGIN.APP"

3. **Footer (تذييل الصفحة)**:
   - يجب أن ترى: "جميع الحقوق محفوظة لشركة ORIGIN.APP"

4. **التقارير**:
   - أي تقرير تطبعه يجب أن يظهر "ORIGIN.APP"

---

## ⚠️ ملاحظات مهمة

1. **إذا لم تتغير الصفحات**:
   - امسح الـ cache في المتصفح (Ctrl+Shift+Delete)
   - جرب Incognito/Private mode
   - تأكد من إعادة تشغيل الخادم فعلياً

2. **الملفات الثابتة القديمة**:
   - إذا كنت تستخدم Nginx/Apache، أعد تشغيلهم أيضاً
   - تأكد من تحديث إعدادات Static Files

3. **الجلسات القديمة**:
   - قد تحتاج إلى تسجيل الخروج والدخول مرة أخرى

---

## 📞 في حالة المشاكل

إذا استمرت المشكلة بعد إعادة التشغيل:

```bash
# تحقق من قاعدة البيانات
cd /home/zakee/homeupdate
python manage.py shell
>>> from accounts.models import CompanyInfo
>>> CompanyInfo.objects.first().name
# يجب أن يظهر: 'ORIGIN.APP'

# مسح كل شيء وإعادة البناء
python manage.py collectstatic --clear --noinput
python manage.py migrate
```

---

✅ **جميع التغييرات جاهزة! فقط أعد تشغيل الخادم.**
